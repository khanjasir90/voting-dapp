<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <title>D-Voting</title>
</head>
<body>
    <center><h1>Welcome to D-Voting</h1></center>
    <center>Your Address : <%= address%></center>
    <br>
    <center>
        <div style=" margin: auto">
            <% if(data)  { %>
                <ul class="list-group">
                    <% data.forEach(function(data) { %>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <%= data.name%>
                        <span class="badge bg-primary rounded-pill"><%= data.voteCount%></span>
                        </li>
                        <br>
                        <% if(!alreadyVoted) {%>
                            <center><a href="/vote/<%= data.id%>" class="btn btn-outline-dark">Vote</a></center>
                        <% } else {%>
                            <center><a href="/vote/<%= data.id%>" class="btn btn-outline-dark disabled" aria-disabled="true">Vote</a></center>
                        <% } %>
                        <br>
                    <% }); %>
                    <br>
                </ul>
            <% } else {%>
                <h1>No Candidates!!! Pls add candidates to start voting</h1>
            <% } %>
        </div>
    </center>
</body>
</html>